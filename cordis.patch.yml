# dsh-login bundle patch: insert the plugin and take over the web runtime.
#
# Applied as a bundle layer when installed via
#   dsh plugin --profile web add github:islibaodong/dsh-login
# (this file is declared in package.json's dsh.bundle.patch).
#
# Accounts: no pre-configuration needed. On first visit the /login page shows
# a setup form that creates the administrator account (username + password,
# scrypt-hashed in the DSH credentials system under ref
# DSH_LOGIN_PASSWORD_USERS); admins then manage users at /admin.
#
# IMPORTANT: dsh-login claims the webserver fallback seat (the single
# seat for everything no named route claims). The dsh-web-app bundle's
# `web-runtime` row mounts the frontend-static fallback owner, so that
# row is disabled here; dsh-login re-provides the webRuntime service
# (LAN trust for the /api fence + DSH_WEB_URL) and serves the dist
# itself through the authenticated gateway.

- insert:
    - id: dsh-login
      name: '@deepseek-ai/dsh-login'
      config:
        password: DSH_LOGIN_PASSWORD
        # distIndex: '' resolves @deepseek-ai/dsh-web-frontend/dist automatically.
        # trustedHosts defaults to []; if you launch with --trusted-host, add
        # the same authorities here (a config row cannot read ctx.webStartup
        # without declaring inject, so this patch pins no expression).
        sessionTtl: 604800
        takeOverWebRuntime: true
        enabled: true

# The web-runtime row's fallback claim conflicts with dsh-login's gateway;
# dsh-login re-provides webRuntime, so disabling it is safe.
- id: web-runtime
  disabled: true

# The shipped /api carrier is replaced by dsh-login's identity-aware takeover
# (`src/connection.ts`, mounted by dsh-login itself through the
# createConnectionPlugin factory — its SessionStore/OwnershipIndex instances
# live inside the dsh-login fiber, so it cannot ride a standalone row). The
# webserver rejects duplicate /api prefix registrations, so the shipped row
# must stay disabled.
#
# Browser half (merge-gate Option A): dsh-login ships its own client bundle.
# The package declares `dsh.client` (platform web, immediately) and exports
# the built artifact `dist/client.js` — a re-stamped copy of the shipped
# connection client bundle (src/connection.client.ts re-exports it verbatim;
# regenerate with `npm run build:client` after upgrading the carrier). The
# client-modules scanner picks browser halves from exactly this pair, so the
# disabled shipped row no longer removes the GUI's wire client from the boot
# graph.
- id: connection
  disabled: true
